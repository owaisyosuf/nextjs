export type blog = {
  data: string;
};

export type ButtonType = {
  Data: string;
};

export type HeadingType = {
  title: string;
};

export type tagType = {
  title: string;
};
export type Cardtype = {
  title: string;
  blog: string;
  button: string;
  tag: string;
};
